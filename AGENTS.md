# AGENTS.md

Persistent memory for working on this project. Read this first.

## Project identity

- **Name:** AstaHub (formerly "Asta 100 Days of C" / asta-100)
- **Mission:** Free, hands-on, world-class technical education for every person on Earth. Learning should be fun, social, human — and free forever.
- **Location:** `D:\🐺 LoneDevWolf\Projects\Asta_100Days_of_C`
- **Vision doc:** `VISION.md` (the full plan: pedagogy, knowledge bank, community, economics, money-from-skills, roadmap). Read it before large work.
- **Original notes:** `PLANS.md` (the seed; VISION.md expands it).

## Current state (as of 2026-08-06)

- Working production app: 100-day C/x86-64 Assembly curriculum, dashboard, playground (Monaco on CDN), achievements, Piston-backed `/api/execute` with an in-browser simulated fallback.
- Curriculum split into `src/lib/curriculum/` — lazy-loaded per-day modules + `core.ts` blueprints. 63 hand-written days (1–61, 81, 100); rest procedurally generated.
- New simulator `src/lib/simulator.ts` (C + ASM) — recently fixed and verified: for loops, if/else-if/else chains, arrays, pointers, bitwise, inc/dec, compound assignment, `} else {` handling. No function-call/struct/scanf modeling (best-effort fallback).
- Performance work done: self-hosted fonts, `LazyScene3D`, lazy curriculum, `optimizePackageImports`, sitemap (`postbuild: next-sitemap`). Home First Load ~137 kB.
- Everything builds green: `npm run build` (works after `Remove-Item -Recurse -Force .next`).
- Auth & data layer live: Postgres (Neon) + Auth.js credentials + bcrypt, register/signin/profile/leaderboard APIs, middleware route protection, progress sync via `/api/progress`. See progress log for the known `next start` middleware EvalError on Node 24 (dev mode works).
- **The whole product now tells the AstaHub story, not the C course story.** Landing hero = "Learn to build. Free. Forever." with a generalized HeroTerminal (coach dialogue, reviews, capstone, streak — no language code). A **tracks hub** (`/tracks` + `/tracks/[slug]`) lists the full knowledge bank from VISION §3.1/§3.2 in three groups (languages, sciences, dream) via a single source of truth `src/lib/tracks.ts`; C/Assembly are live, everything else is an honest "on the roadmap" page. Nav: **Learn** dropdown + **My journey** (/dashboard) + **Practice** (/playground); tier glyphs removed; standard slide-in mobile drawer. Footer gained a **Knowledge bank** column. Curriculum page renamed "The tracks" (still the C/ASM day list). Committed + pushed to `main` as `8fb55eb`.

## Design direction (APPLIED — keep going in this direction)

- **Applied.** The UI is now **pure black & white minimalism with mature energy** — black bg (`#000000`), white as the sole accent, hairline borders, typography-driven, content-first. All old accent colors are remapped to monochrome: `cyber-cyan` = white, `cyber-dark` = black, `matrix` = neutral gray ramp, tier colors = grayscale peaking at white for Master. Only `cyber-red` remains, for errors (semantic). Landing page is ONE section: mission headline + interactive typing terminal + principles + knowledge-bank tracks list + stats + closing mission line.
- **Typography:** Space Grotesk (display), Inter (body), JetBrains Mono (code) — all **self-hosted** in `public/fonts/` via `@font-face` (no `next/font/google`, which was causing flaky build-time network hangs).
- **Black-and-white energy rule:** monochrome only. White marks the current action/state and the top tier. Red is reserved for errors. Never reintroduce color accents on new pages/components.
- **Theme system:** dark is default; a light theme exists (`data-theme="light"` on `<html>`, `localStorage["asta-theme"]`). All colors run through CSS variables (`--bg`, `--fg`, `--text-a/b/c`, `--surface`, `--line`, `--danger`, `--tier-1..5`) mapped in `tailwind.config.ts` via `rgb(var(--x) / <alpha-value>)`. Use token classes (`text-white`, `gray-*`, `border-white/5`, `cyber.*`), never hard-coded hex, so new UI stays theme-safe. Tier colors are `var(--tier-N)` — never concatenate alpha suffixes onto them; use `color-mix(in srgb, var(--tier-N) X%, transparent)`.
- Reference sites: Linear, Stripe, Notion, Vercel, Typeform, The Odin Project, freeCodeCamp; galleries: lapa.ninja/category/education, a1.gallery/websites/minimal-landing.

## Tech stack (planned — not yet migrated)

- **Now:** Next.js 15.5.22 App Router, React, TypeScript, Tailwind, PWA manifest.
- **Phase 0 (next):** PostgreSQL + Prisma, Auth.js, Redis (Upstash), Cloudflare R2 — for accounts, profiles, progress, leaderboards, certificates.
- **Later:** Pyodide, Socket.io realtime, Cloudflare Stream + YouTube, Vercel AI SDK hint-ladder coach, Meilisearch.
- See VISION.md §7.3 for the full table.

## Roadmap (phases)

| Phase | What | Why this order |
|-------|------|----------------|
| **0 — Foundation** | Redesign (minimal theme), accounts, profiles/avatars, progress sync, leaderboard, certificates | Turns a course into an institution; nothing works without identity |
| **1 — Breadth** | Python, then JS/TS, then Rust through the same engine | Max knowledge-per-effort; validates the "hub" idea |
| **2 — Community** | Posts, Q&A, groups, chat — moderation budgeted | Human layer; makes learning social and sticky |
| **3 — Live** | Live coding, YouTube export, live classes | The "real human feeling" |
| **4 — AI Companion** | Hint ladders, rainchecks, adaptive review | The differentiator; deserves focus |
| **5 — The Sciences** | Math, physics, EE, ML/AI, security, full-stack tracks | Content projects on a proven engine |
| **6 — The Dream** | Humanities, arts, the rest of human knowledge | After technical excellence |

Full detail: VISION.md §12.

## Build & verify commands (Windows / pwsh)

- Dev: `npm run dev`
- Clean prod build: `Remove-Item -Recurse -Force .next; npm run build` (postbuild regenerates sitemap)
- Run prod: `npm run start`
- Quick simulator iteration: `node node_modules\typescript\bin\tsc src\lib\simulator.ts --outDir C:\Users\ok\AppData\Local\Temp\opencode\simtest --module commonjs --target es2020 --moduleResolution node` then copy to `simulator.cjs`; run scenarios via `C:\Users\ok\AppData\Local\Temp\opencode\simtest\test2.cjs`.
- API smoke test: start `npx next start -p 3999`, `POST /api/execute` with `{code, language}`; kill node afterwards.
- Note: PowerShell console mangles UTF-8 box-drawing chars (──, —) — it's terminal rendering, not a bug. Verify bytes when unsure.

## Progress log

Track significant work here as it happens. Format: date — what was done — outcome.

- 2026-08-16 — **`expectedOutput` for all generated code challenges + simulator fixes (applied).** Roadmap P1 item done. (a) New `PY_EXPECTED_OUTPUT` (24/40) and `CPP_EXPECTED_OUTPUT` (38/40) maps in `src/lib/curriculum/{python,cpp}/core.ts` wire **real runtime output** into every deterministic generated code challenge — computed empirically by running all 80 templates through local python3 and g++ (values are the exact first-line substrings a correct run prints). The `expectedOutput` gate now applies to every track, not just hand-written C days. (b) **Python gate is double-safe**: the 24 gated templates are each verified reproducible by the in-browser simulator (new test asserts `simulateAnsi` output contains `expectedOutput`), so verification works in BOTH real and simulated mode. The 16 ungated python days (6/21 stdin, 25/26 env-dependent, 13/17/18/23/29/32–37/39 OOP/lambda/generator/decorator/type-hint features the subset sim can't model) stay ungated by design. (c) **C++ gate is real-compiler-only**: 38/40 days gated (day 6 needs stdin, day 34 is a multi-file stub); there is no in-browser C++ simulator. (d) **Simulator fixes**: `sorted(..., reverse=True)` now honored (PY_28 gateable), `json.dumps` now emits Python-style separators `", "`/`": "` (PY_24 gateable + faithful output), and the **C++ fallback no longer fakes NASM output** — `simulateAnsi` for `cpp` returns an honest "no in-browser C++ simulator yet — connect Piston" message instead of garbage. (e) Fixed a pre-existing `tsc` failure: `ProgressInput` now declares `level?: unknown` (sent by client, deliberately ignored — the security posture was right, the type just didn't say so). **Important finding**: the public Piston API (`emkc.org`) is now **whitelist-only (401) since 2026-02-15** — the configured `PISTON_AUTH_TOKEN` no longer works; `/api/execute` verified to degrade gracefully to the simulator (`real: false`). C++ cannot run for real until a self-hosted Piston instance exists. Verified: `npx tsc --noEmit` clean (was failing on the pre-existing `level` type), `npm run lint` clean, `npm test` **49/49 green** (3 new: python gate sim-reproducible, cpp gate non-empty, all-track code-id uniqueness), clean `next build` green (21 pages), live `/api/execute` smoke: python sim returns `Hello, World!` (matches PY_1 gate), cpp returns the honest fallback. Also raised default `testTimeout` to 30s in `vitest.config.mts` — the curriculum integrity test (100 lazy day imports) exceeds the 5s default on this slow machine and flaked CI-style runs. Next per roadmap: self-host Piston (§18/backlog 11), API route tests, JS/TS track.
- 2026-08-16 — **Phase 0 AUDIT + ENGINEERING_ROADMAP.md + P0 security fixes (applied, committed).** Ran the full repository audit per the master engineering mandate. Findings: uncommitted verified work from 08-12/08-13 was sitting unprotected in the working tree — **committed in 4 logical units** (`06663a7` hand-written days 62–80/82–99, `8a0fcd3` framer-motion cut + curriculum de-heavying, `04b82d5` vitest+CI, `24febc2` generated quiz-ID fix). Created **`ENGINEERING_ROADMAP.md`** — the living technical roadmap (§1 state, §2 architecture, §3–§19 debt/bugs/security/perf/engine/curriculum/assessment/gamification/identity/certificates/community/AI/graph/infra/testing/deploy/future + prioritized backlog). **P0/P1 fixes applied**: (a) `src/lib/store.ts` `completeDay` no longer double-awards XP when a day is already complete; (b) new `src/lib/progressValidation.ts` — server-side clamp of `totalXp`/`streak`/`currentDay`, sanitized+deduplicated `completedDays` per track, `level` now **derived from XP server-side** (client can no longer inflate leaderboard/level); `/api/progress` refactored onto it; (c) new `src/lib/rateLimit.ts` (in-process sliding-window) + `/api/execute` hardening (rate limit 30/min/IP, 128 KB body cap, 50 KB code cap, 429/413 responses, JSON parse guard); `/api/register` hardening (email format, name 2–60, password ≤128, rate limit 5/10min); (d) **PWA icons fixed** — `public/icon-192.png` + `icon-512.png` generated (manifest referenced missing files). Added `tests/progress.test.ts` (12 tests: sanitize/clamp/dedup/level-derivation/rate-limit). **README rewritten** from the stale cyberpunk/Next-14/3D description to the real AstaHub product; `package.json` metadata refreshed (repo → AridsWolfgang/AstaHub). Verified: `tsc --noEmit` clean, lint clean, `npm test` **40/40 green**, clean `next build` green (all 13 route groups + sitemap regenerated). Git log now clean and conventional. Next per roadmap: `expectedOutput` for python/cpp code challenges, store/API unit tests, JS/TS track, runtime pin (Node 20/22), Redis rate limiting.
- 2026-08-13 — **All 100 C/ASM days now hand-written (applied).** Filled the last 37 stub days (62–80, 82–99) with full hand-crafted lessons, replacing the `const lesson: Partial<Lesson> = {}` blueprints: each has 3 dense theory sections (≥1 with a NASM/C `codeExample`), a runnable playground, 2 quizzes + 1 code exercise with unique `d{N}-q1/q2/c1` IDs, and a `d{N}-a1` assignment with rubric. Topics: string ops (LODS/STOS/MOVS/SCAS/CMPS), SysV ABI, reading C in asm, arrays/structs in asm, bit manipulation, FP/SSE, SIMD, interrupts, GCC inline asm (`language: "c"` for day 71 only), optimizing (pipeline/branch-pred/unrolling), reverse engineering, shellcode, bootloaders, segmentation/GDT, paging, clone/threads/TLS, drivers (port I/O + MMIO), crypto (XOR/AES-NI/timing), RDTSC/CPUID, SEH, PIC/GOT/PLT, ELF/linker scripts, GDB, C↔ASM interop, atomics, memory barriers, AVX-512, VMX, security/ROP, obfuscation, RT systems, UEFI, SMP/APIC, ARM64, perf profiling, capstone prep. Written in 4 parallel subagent batches (one batch failed and was re-done in-session). Verified: **zero stubs remain** (all 100 day files ≥5KB), `npx tsc --noEmit` clean, `npm test` 28/28 green (curriculum integrity now exercises all 180 lessons), clean `next build` green, `/lesson/{62,71,80,90,99}` route correctly 307→signin (middleware-protected, no 404/500), lessons resolve with 3 sections + 3 exercises + assignment.
- 2026-08-12 — **Performance: framer-motion cut to the landing page only (applied).** Removed `framer-motion` from 8 files (Navbar dropdown+mobile drawer → CSS transitions, LessonView tab swaps → plain div, ProgressRing, CurriculumBrowser, TrackJourney, dashboard, achievements, certificates); kept only on `/` for the count-up stats/scroll reveals. Every non-landing page's First Load JS dropped ~40 kB gzipped (achievements 154→114, certificates 150→110, curriculum 155→115, dashboard 157→117, lesson 163→122, tracks/[slug] 156→116); clean build time 96s→58s. Also fixed a stale `.next`-caused 500 on `localhost:3000` (restarted the server; recipe: stop → `Remove-Item .next` → `npm run build` → `npm run start`).
- 2026-08-12 — **Quality & performance pass: tests, CI, two duplicate-ID bug fixes, curriculum page de-heavying (applied).** Responding to a frank codebase review: the interpreter + content engine had no tests, no CI, and the curriculum page shipped all 100 full lessons (338KB) to the client. (a) **Vitest harness**: `vitest` dev dep + `vitest.config.mts` + `npm test`/`test:watch` scripts. `tests/simulator.test.ts` = 20 scenarios pinned from the ad-hoc temp runs (C for loops, `sum=15`, if/else-if/else chains, arrays, pointers, bitwise, pre/post-increment, while loops; Python factorial=120, if/elif/else, slicing, dicts, `end=`/`sep=`, list comprehensions, while/break, short-circuit; ASM hello; output shape) using a `body()` helper that strips the "ASTA Runner" banner (which contains "A", breaking `not.toContain("A")` assertions). (b) **`tests/curriculum.test.ts`** = content integrity for all 180 lessons (100 C/ASM + 40 Python + 40 C++): every day resolves a well-formed lesson (title/subtitle/tags/level, non-empty theory sections, ≥1 exercise with unique IDs, valid quiz/code shapes, code exercises require starterCode). **Caught and fixed two real bugs**: duplicate exercise IDs (`cppN-q2`, `pyN-q2`) in the Python and C++ quiz generators when the first topic had no quiz-map entry — generators now derive IDs from `quizzes.length + 1` instead of the loop index (`i + 1`). (c) **GitHub Actions CI** (`.github/workflows/ci.yml`): `npm ci` → lint → `tsc --noEmit` → `npm test` → `npm run build` on Node 20. (d) **Curriculum page de-heavied**: `src/app/curriculum/page.tsx` is now a server component that builds a lightweight `LessonMeta` index at build time via new `getLessonMetadata()` in `src/lib/curriculum/index.ts`; the client filter/search/tier UI moved to `src/components/CurriculumBrowser.tsx` receiving only metadata — the 338KB of full lesson bodies no longer ships to the browser (route dropped 6.18kB→4.71kB). Verified: `npm test` 28/28 green, `npx next lint` clean, `npx tsc --noEmit` clean, clean `next build` green (21 pages), prod `/curriculum` 200 and renders lessons server-side. Not done (deferred by design): simulator still regex-based (kept as labeled fallback; Piston remains primary), Python/C++ kept as live tracks, `expectedOutput` still optional on generated code challenges.
- 2026-08-08 — **Phase 0 lint cleanup: `<img>` → `next/image` (applied).** Cleared the last two `@next/next/no-img-element` warnings in `src/app/leaderboard/page.tsx` (Avatar) and `src/components/ProfileClient.tsx` (Avatar) by switching to `next/image` with `unoptimized` (correct for user-supplied avatar URLs; auth is credentials-only so `image` stays null today). `npx next lint` clean, `npx tsc --noEmit` clean, clean `next build` green (21 pages). **Phase 0 is now feature-complete**: progress sync works for all three tracks (C on `User`, python/cpp on `UserTrackProgress`; stores carry `sync: true`), track-aware dashboard/achievements/leaderboard/settings/certificates/export all live, certificates auto-issued in `/api/progress` PUT, code-exercise verification gates "Mark Complete" on `expectedOutput`. Verified state: the dashboard reads stats from the selected track's own store (`STORES[track]`), `/api/export` includes all tracks + certs. No lint warnings remain.
- 2026-08-08 — **Python/C++ got a progress home (TrackJourney, applied).** Added `src/components/TrackJourney.tsx` (client) — a "Your journey" section with live stats (days done/total, XP, streak, tier), a Start/Continue CTA that jumps to the first unlocked incomplete day, and a full day grid with lock/done states and per-day exercise/assignment marks, wired to the track's own store (`usePythonStore`/`useCppStore`). Embedded it in `/tracks/[slug]` for live python/cpp tracks, closing the gap where Python/C++ learners had nowhere to see progress (dashboard/achievements/leaderboard remain C-only). `npx tsc --noEmit` clean; clean `next build` green (routes unchanged); prod `next start` smoke-tested 200 on `/tracks`, `/tracks/python`, `/tracks/cpp`, `/lesson/python/1`, `/lesson/cpp/1`. Also confirmed `next start` no longer hits the middleware EvalError on Node 24 (may be resolved by the clean rebuild). Known: dashboard/achievements/leaderboard still C-track-only; code-exercise "Mark Complete" doesn't verify `expectedOutput`.
- 2026-08-08 — **Phase 0 completion set (applied, committed `881c5e8`, pushed to `main`).** (a) **Code-exercise verification**: `CodePlayground` gained `onRunOutput` callback; `LessonView` records per-exercise run output and gates "Mark Complete" on `expectedOutput` substring match (button disabled + "Run your code to verify" until the last run matches; applies where `expectedOutput` is defined — all hand-written C days; python/cpp generated challenges have none so stay ungated). (b) **Track-aware dashboard**: `/dashboard` reads `?track=python|cpp` (default c), switches store (`usePythonStore`/`useCppStore`), loads lessons via `getTrackLesson`+`getTrackTotalDays`, links to `/lesson/{track}/{day}`, with a C/Python/C++ pill switcher. (c) **Track-aware achievements**: `/achievements` has per-track achievement sets (C=14 original, Python=16, C++=13 mapped to real day topics) + switcher; tier strip unchanged. (d) **Track-aware leaderboard**: `/api/leaderboard?track=python|cpp` ranks via `UserTrackProgress` (invalid track falls back to C); page became a client component with switcher + loading/empty states. (e) **Settings** `/settings` (middleware-protected): password change via new `POST /api/password` (bcrypt verify+hash), export journey via new `GET /api/export` (profile + all track progress + certificates as downloadable JSON), delete account via `DELETE /api/me` (cascades) + signOut. (f) **Certificates** `/certificates` (middleware-protected): lists earned certs with print; **auto-issued** in `/api/progress` PUT when a track's `completedDays` covers all days (c=100, python=40, cpp=40), upserted per `(userId, track)`. **DB migration `20260807232942_track_progress_and_certificates`** applied to Neon (creates `UserTrackProgress` — was previously schema-only, and adds `Certificate.track`); requires `$env:DATABASE_URL` from `.env.local` (Prisma CLI doesn't read `.env.local`). Navbar + footer gained Certificates/Settings under Account. Verified: `npx tsc --noEmit` clean; clean `next build` green (a stale `.next` from a lint-failed build caused `/ _not-found` — always full clean after a failed build); `next start` smoke: `/settings` `/certificates` `/dashboard` `/achievements` → 307 signin, `/leaderboard` `/tracks` `/api/leaderboard?track=python` → 200. Known: python/cpp progress syncs only after a browser visit (no issue), C `completedDays` stored on `User` (certificate C works); `<img>` lint warnings pre-existing.
- 2026-08-07 — **Python simulator now works (17/17 scenarios green).** Fixed the Python simulator in `src/lib/simulator.ts`: `resolveName` falls back to returning the builtin name string so `callPy` dispatches `print`/`len`/`range` etc.; operators (`+ - * / // % ** < > <= >= == != in is and or not`) are now parsed as **lazy `__binop` nodes** resolved against scope in `resolveExpr` (were eagerly applied to unresolved `__name` nodes); `and`/`or` short-circuit returning operands; `not` unary; `is not`; unary minus lazy. Rewrote **if/elif/else** execution as a chain gatherer (old code's `while`-loop `continue` left `i` on the else header, executing the else body as a top-level statement — printed both B and C). Fixed `name[0:3]`/`[:3]` slicing (parseAtom was consuming the `:`), dict index-assign stored the raw key text including quotes (`"city"`), string lists/dicts now render with Python-style single-quote repr (`pyRepr`), `end=`/`sep=` kwargs honored via a growing output buffer (`outStr`) so `print(i, end=" ")` stays on one line, list comprehensions `[expr for x in iterable if cond]` (added `for`/`if` to tokenizer keywords; ranges expanded via `rangeValues`), `pyEq`/`pyToStr`/`list(range)` null-safety, `Record<number,…>`→`Record<string,…>` for `completedExercises`/`notes` in `types.ts`+`store.ts`. **Wired Python into the `/api/execute` simulator fallback** (previously "Python has no in-browser simulator yet"). Verified: 17/17 python scenarios pass, C simulator still `sum=15`, `npx tsc --noEmit` clean, clean `next build` green, live `/api/execute` python smoke test returns `120` (factorial(5)) via simulation. Known: python/cpp stores still not server-synced; `next start` middleware EvalError on Node 24.
- 2026-08-07 — **C++ track + shared lesson engine + dependency cleanup (applied).** Added a live **C++ track** (`/lesson/cpp/[day]`, 40 days) mirroring the Python pattern: `src/lib/curriculum/cpp/core.ts` (40 blueprints + quiz map + `buildCppLesson`), loader `cpp/index.ts`, routing in `curriculum/index.ts` (`TOTAL_TRACKS = { c: 100, python: 40, cpp: 40 }`), `useCppStore` (`asta-cpp-progress`, `sync: false`), shared `src/components/LessonView.tsx` handling c/python/cpp, `api/execute` Piston `"c++"` mapping, playground templates + `C++20` badge, track entry in `tracks.ts` (live). Added missing quiz-map entries so all 40 days yield 3+ exercises. **Removed dead 3D deps** (`three`, `@react-three/fiber`, `@react-three/drei`, `@types/three`) + orphaned `Scene3D.tsx`/`LazyScene3D.tsx` (were never imported) and `@prisma/adapter-pg` (unused; plain `PrismaClient`). 88 packages removed from `node_modules`. `npx tsc --noEmit` clean; clean `next build` green (17 pages, /lesson/cpp/[day] in route table); dev smoke-tested 200 (`/tracks`, `/tracks/cpp`, `/lesson/cpp/1`, `/lesson/cpp/40`, `/playground`). Known: python/cpp stores not server-synced; python has no simulator fallback; `next start` middleware EvalError on Node 24.
- 2026-08-06 — **AstaHub story refactor: landing v3 + tracks hub + nav/footer (applied, committed `8fb55eb`, pushed to `main`).** Landing hero is now "Learn to build. Free. Forever." — no language code anywhere; **HeroTerminal** reworked to show the learning mechanic (coach dialogue, reviews, capstone, streak), typing slowed to 26ms/char, output 500ms/line, hold 3.8s. Added **tracks hub** `/tracks` + `/tracks/[slug]` listing the full knowledge bank from VISION §3.1/§3.2 in three groups (languages, sciences, dream) via single source of truth `src/lib/tracks.ts`; C/Assembly are live, everything else is an honest "on the roadmap" page; sections have id anchors + `scroll-mt-20`; unknown slug renders the 404 page. **Navbar rewritten**: Learn dropdown + **My journey** (`/dashboard`) + **Practice** (`/playground`); tier glyphs removed; mobile drawer became a standard slide-in (86% width, max-w-sm, backdrop, framer-motion, body scroll-lock); Achievements moved into the Account group. **Footer**: brand + mission line, Learn column (My journey/All tracks/Practice), Knowledge bank column, Account links. Fixes: `Logo.tsx` `textAnchor`, `layout.tsx` `suppressHydrationWarning`. **Root-caused dev slowness to hardware** (2012 i7-3540M, 4 threads, 7.9 GB RAM; Next 15.5.22 dev webpack ~17s boot, 60–80s first compile of 1339 modules) — turbopack suggested, user declined testing; verify via `next build` + `next start` instead of slow dev. `npx tsc --noEmit` clean; routes smoke-tested 200 (`/tracks`, `/tracks/c`, `/tracks/python`, `/tracks/mathematics`, `/tracks/humanities`). `git push origin main` confirmed (`ac48d69..8fb55eb`).
- 2026-08-06 — **Logo refactor + navbar polish (applied).** New reusable `src/components/Logo.tsx`: `LogoMark` (inline SVG replicating `public/favicon.svg` — black rounded square + white monospace "A", theme-safe) + `Logo` (Link to `/` with wordmark + optional tagline). `public/favicon.svg` updated to monochrome (was cyan `#00f0ff`). Navbar now uses `Logo`; mobile drawer polished: body scroll-lock while open, Escape closes, authed users get a name/email chip + "Account" section (Profile/Leaderboard/Sign out), guests get a "Getting started" panel with sign-in CTA, stats bar kept. Footer + signin page now use `LogoMark` instead of lucide `Cpu`. `npx tsc --noEmit` clean; prod build compiled successfully.
- 2026-08-06 — **Dark/light theme system + landing page v2 (applied).** Added a global theme toggle (dark default, `data-theme="light"` on `<html>`, persisted via `localStorage["asta-theme"]`, pre-hydration inline script in `<body>` to kill FOUC). Made the whole app theme-aware with CSS variables (`--bg`, `--fg`, `--text-a/b/c`, `--surface`, `--line`, `--danger`, `--tier-1..5`) mapped through Tailwind by overriding `white`/`black`/`gray` + `cyber.*` tokens to `rgb(var(--x) / <alpha-value>)` so every existing `text-white/border-white/gray-*` usage flips correctly in both themes; tier colors became `var(--tier-N)` (fixed the alpha-suffix sites with `color-mix`, ProgressRing stroke via `style`). **Rebuilt the landing page as ONE section, light and straightforward**: two-column hero (copy + interactive **HeroTerminal**), inline count-up stats, 4 clean principle cards (no expand gimmick), a mobile-scrollable/hoverable 100-day journey strip (`no-scrollbar` snap scroll on mobile, 5-col grid on desktop) with "your current tier" marker + progress line + detail panel, and a closing mission CTA. **Removed the 3D scene (LazyScene3D) from the landing** — the heavy three.js chunk no longer ships on home (Home first load 150 kB). **Rewrote Navbar** per VISION §7.4: slim, hairline, minimal wordmark + tagline, quiet links, theme toggle, auth cluster, clean mobile menu with XP/streak/tier. **Added a global minimal Footer** (brand + mission line, Learn/Account link columns, mission tagline + copyright) to the root layout. Theme toggle also added to all pages via the Navbar. Prod build green, no cyan, no runtime errors, all routes 200. Known quirk: `next dev` overwrites `.next` with eval-source-map dev chunks (6 MB `main-app.js`) — always re-run `npm run build` to restore prod numbers.
- 2026-08-06 — **Pure black & white redesign + mission narrative (applied).** Remapped all design tokens to monochrome: `cyber-cyan`→`#ffffff`, `cyber-dark`→`#000000`, `matrix`→neutral gray ramp, tier colors→grayscale peaking at white for Master; only `cyber-red` stays (errors). Swapped Orbitron → **Space Grotesk** (display) and added **Inter** (body); self-hosted all fonts in `public/fonts/` via `@font-face` from fontsource/jsdelivr and removed `next/font/google` (was hanging builds with `socket hang up` on fonts.gstatic.com). Rewrote **landing page as ONE section** with mission narrative ("Learn C and Assembly. Free. Forever."), new interactive **HeroTerminal** (auto-typing C/ASM demos with simulated output), click-to-expand principle cards, hoverable 100-day tier strip with "your current tier" marker + progress line + count-up stats. Rewrote page copy to the mission voice (Navbar tagline "Free Technical Education", dashboard "Today is your next day", curriculum "Two languages. One honest road to the metal."). Replaced emoji icons with monochrome lucide icons (achievements, leaderboard medals, streak flame). Prod build green, no cyan in compiled CSS, all routes smoke-tested 200.
- 2026-08-06 — Wrote `VISION.md` (expanded AstaHub vision), added tech stack (§7.3), design language (§7.4), money-from-skills (§9). Named the project **AstaHub**.
- 2026-08-06 — Fixed C simulator bugs: non-greedy control-statement regex, `i++`/compound assignments, `unsigned`/`long` types, `&` address-of vs bitwise, `} else {` line splitting, pre/post-increment in expressions. Verified all scenarios; prod build + live API smoke test pass.
- 2026-08-06 — Added AGENTS.md as persistent memory, roadmap phases, design direction + reference sites (Linear, Stripe, Notion, Vercel, Typeform, The Odin Project, freeCodeCamp).
- 2026-08-06 — **Phase 0 redesign applied.** Replaced cyberpunk theme with calm editorial minimalism: new `tailwind.config.ts` palette (cyan accent `#22d3ee`, warm-dark bg `#0a0e14`, emerald matrix), rewrote `globals.css` (removed scanlines/glows/grid, added `.btn`, `.btn-primary`, `.input`, `.hairline`), kept old token names (`cyber-*`, `matrix-500`, `btn-cyber*`, `text-glow-*`) as minimal aliases so ~100 usages inherited the look without edits. Stripped glow shadows from `CyberPanel`, removed pulse-glow from `Navbar`, softened `Scene3D` (slower motion, new palette). Prod build green (route sizes unchanged, ~88.8 kB shared); smoke test confirms no scanline/grid/neon remain and new accent renders.
- 2026-08-06 — **Phase 0 auth & data layer scaffolded (Postgres + Auth.js).** Installed `next-auth@4.24.7`, `@prisma/client@6.19.3`, `prisma@6.19.3` (dev), `bcryptjs@3.0.3`. Schema `prisma/schema.prisma`: `User` (name, email, passwordHash, bio, image, currentDay, totalXp, level, streak, lastActiveDate, completedDays/Notes/Exercises JSON, updatedAt) + `Certificate` (cascade, indexed by userId). Built `src/lib/prisma.ts` + `src/lib/auth.ts` (Credentials/JWT, bcrypt), APIs `/api/auth/[...nextauth]`, `/api/register`, `/api/me` (GET/PATCH), `/api/progress` (PUT), `/api/leaderboard` (GET, top 100), `src/middleware.ts` (protects dashboard/profile/achievements/lesson). Pages: `/signin` (signin+register tabs), `/profile` (server, redirects if unauthed) + `ProfileClient` (edit name/bio, stat cards, completion bar, certificates), `/leaderboard` (server, podium + ranked rows + "(you)"). Store: `queueSync` debounced 800ms → PUT `/api/progress`, `hydrateFromServer` (GET `/api/me` → setState), `markSynced`/`markUnsynced`, gated by localStorage flag `asta-100days-synced`; `SessionProvider` hydrates on session load; Navbar shows Profile/Leaderboard/Sign-in/out. Fixed 3 type errors (next-auth Session augmentation `src/types/next-auth.d.ts`, `Record<string, string|null>` in /api/me, `level as ProficiencyLevel` + `issuedAt: string|Date` in profile/leaderboard). Prod build green (~88.8 kB shared). **Awaiting real `DATABASE_URL` to run migration + live auth test.**
- 2026-08-06 — **Phase 0 live: Postgres + Auth.js verified end-to-end.** User provided Neon URL (`ep-lucky-meadow-ay684ucq-pooler...neon.tech`). `prisma migrate dev --name init` applied (migration `20260806105340_init`). `.env.local` now has real `DATABASE_URL` + generated `NEXTAUTH_SECRET`. Verified on dev server (port 3998): `/signin` 200, `POST /api/register` 201 (user persisted to Neon, `users: 1`), `/api/leaderboard` returns ranked user, `/profile` + `/dashboard` → 307 `/signin?callbackUrl=...` via middleware. **Known issue: `next start` (prod) throws `EvalError: Code generation from strings disallowed` from the edge middleware sandbox on Node v24.14.1** — auth works in dev; needs Node 20/22 or Next.js upgrade for prod middleware. Neon cold-start causes first DB call per server to take ~8s (retry succeeds).
- 2026-08-06 — **Black/white editorial UI pass (Phase 0 design, applied).** Enforced the one-cyan-accent system across every page: `PROFICIENCY_TIERS` colors now a monochrome grayscale ramp (`#64748b`→`#e2e8f0`) peaking at cyan `#22d3ee` for Master; `CyberPanel` glow prop collapsed to one calm hairline (all `glow=` values identical); `ProgressRing` drop-shadow glow removed; Home hero rewritten ("Learn C and Assembly. In 100 days.") with flat monochrome feature cards and no purple/amber/red numbers; Dashboard renamed "Command Center"→"Dashboard", "Mission Timeline"→"Your Days", stats toned to cyan/white, done-states switched from matrix-green to cyan; Navbar XP cyan + streak white; ASM language badges now gray instead of purple everywhere; lesson page success states green→cyan (red kept for errors only), hints/points/purple glow cleaned; CodePlayground Run button now solid cyan (`btn`-style), output text white, "Live/Simulated" badges cyan/gray; curriculum/achievements/not-found/error/loading pages toned to match. Only `cyber-red` remains (error states, semantically correct). Prod build green (~88.8 kB shared).
